# Thrifts Landing — Full Update

This archive includes your original project plus:
- `/components/ThriftsLandingPage.tsx` — full single-file preview
- Preview route at `/app/landing-preview/page.tsx` (visit `/landing-preview`)
- Sectionized components: `QuickStatsRow`, `PersonaSplit`, `SocialProof`
- `content/landing.json` copy updates (hero, how-it-works, FAQ)
- Gentle integration in `app/page.tsx` (backup at `app/page.backup.tsx`)

If anything looks off, you can restore `app/page.backup.tsx`.

